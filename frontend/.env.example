# Base URL of the CodeMap backend API. Defaults to http://localhost:8000
# for local development if unset -- set this explicitly in production
# (e.g. https://api.your-domain.com) rather than relying on the fallback.
VITE_API_BASE_URL=http://localhost:8000
